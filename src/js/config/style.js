const GRID_SIZE = 16

const HEX_SIZES = {
  xl: GRID_SIZE * 12,
  lg: GRID_SIZE * 10,
  md: GRID_SIZE * 8,
  sm: GRID_SIZE * 6,
}

const SCREEN_BREAKPOINTS = {
  xl: GRID_SIZE * 100,
  lg: GRID_SIZE * 80,
  md: GRID_SIZE * 60,
  sm: GRID_SIZE * 40,
}

const STYLE_VARS = {
  GalleryItem_hoverScale: 2,
}

export { HEX_SIZES, SCREEN_BREAKPOINTS, STYLE_VARS }
